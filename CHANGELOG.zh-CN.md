# 更新日志

[English](CHANGELOG.md) · [简体中文（规范源）](CHANGELOG.zh-CN.md) · [繁體中文](docs/CHANGELOG.zh-TW.md) · [日本語](docs/CHANGELOG.ja.md) · [한국어](docs/CHANGELOG.ko.md) · [Español](docs/CHANGELOG.es.md) · [Français](docs/CHANGELOG.fr.md) · [Deutsch](docs/CHANGELOG.de.md) · [Русский](docs/CHANGELOG.ru.md)

## [1.0.4] - 2026-08-20
- 按快捷键切换标签页时（此时未激活标签页），标签外围加上蓝色框框，有助于识别

## [1.0.3] - 2026-08-20
- bug修复：部分情况下点击"设置"等特殊标签页无法切换的问题
- 更改快捷键切换、激活标签的逻辑，原先是按住快捷键就切换并激活新标签，现改为按住快捷键后只切换标签但不激活，按住空格、回测后激活标签
	- 为什么要改？因为原方案在vscode中非常卡，切换的速度慢，如下，A.txt要切换到E.txt就要切换4次标签页，卡成狗了，新方案不卡
```
A.txt
B.txt
C.txt
D.txt
E.txt
```

## [1.0.2] - 2026-07-27
### 问题修复
-  右键菜单，去除悬停1秒弹出子菜单的逻辑，只能点击鼠标弹出
## [1.0.1] - 2026-07-27
### 功能更新
-  <mark>键盘移动标签的功能功能完善</mark>
	- verticalTabs.focus(默认快捷键ctrl+shift+v) 可激活垂直菜单栏标签的焦点
	- 焦点在垂直菜单栏标签时，使用上下箭头、Home、End移动
	- 使用场景
		- 当你想用键盘切换到一个远一点的标签时，可以按 ctrl+shift+v ，然后使用上下箭头、Home、End按键快速移动到你所需的标签，按空格进入
	- 注： 默认快捷键ctrl+shift+v可能与你其它软件的快捷键冲突，比如 YoudaoDict
- 状态栏中的图标从右边改到左边，点击此图标可显示、隐藏标签页
- 右键菜单加上移动分组功能
- 

### 问题修复
- 修复一些已知问题（一堆小问题，懒得列了）
## [1.0.0] - 2026-07-23

<mark>版本大更新，由于更新的内容太多，如遇到bug，可考虑退回0.2.1版本</mark>

### 重点功能说明
- <mark>垂直便签页中的右键菜单可显示VS Code 横向便签页中的所有按钮，包括官方、第三方扩展的按钮</mark>
- 界面样式更新，贴近vscode主题风格
- 可保存、加载工作集
- 垂直便签页可切换位置为左侧或右侧
- 加强搜索功能
- 新增 `verticalTabs.relativePathDisplay` 配置，可配置什么情况，标签可显示路径，比如 仅同名文件显示所在目录名
- 新增多个可配置的快捷键，支持标签页切换、移动等
	- 具体内容可自行查看快捷键说明
	- 默认不绑定快捷键，有需要的自行绑定
	- 跨分组上一个verticalTabs.previousAcrossGroups、下一个标签verticalTabs.nextAcrossGroups，<mark>这两个快捷键非常常用，建议绑定到 ctrl + tab、ctrl + shift + tab，覆盖vscode的默认快捷键</mark>

- 新增多个配置项，具体内容自行查看
### VS Code 原生右键菜单集成

- 标签右键菜单现在可以显示 VS Code 本身以及其他扩展注册到原生编辑器标签菜单的操作。
- 新增 `verticalTabs.showNativeContextMenuActions` 开关，是否启用VS Code 原生右键菜单，默认启用。
- 支持通过键盘进入和操作原生子菜单。
- 备注：

### 标签搜索与定位

- 新增实时标签搜索，可按标签名称过滤。
- 可选择搜索工作区相对路径，并在结果中显示、高亮匹配路径。
- 支持正则表达式搜索；无效正则会提示错误，不会破坏当前列表。
- 显示匹配标签数和匹配分组数，并高亮命中文字。
- 搜索时自动展开包含结果的分组；清空搜索后恢复原有折叠状态。

### 路径显示与同名文件消歧

新增 `verticalTabs.relativePathDisplay` 配置，最终提供五种模式：

- 不显示路径。
- 仅同名文件显示所在目录名。
- 仅同名文件显示工作区相对路径。
- 所有文件始终显示目录名。
- 所有文件始终显示工作区相对路径。

路径显示在标签名称下方；工作区根文件和工作区外文件会使用可识别的父目录信息作为补充。

### 标签导航、排序与移动

- 新增“最近使用”排序模式，按照标签最后一次成功激活时间进行全局 MRU 排序。
- 新打开并激活的标签会成为最近使用项；未激活标签保留稳定顺序。
- 新增“始终跟随活动标签”配置：切换编辑器后自动展开对应分组，并将活动标签滚动到可见区域。
- 新增八个可配置命令：
    - 组内上一个、下一个标签。
    - 跨分组上一个、下一个标签。
    - 在当前分组内向上、向下移动标签。
    - 移动到上一个、下一个分组。
- 移动命令支持多选，并保持被选标签之间的相对顺序。
- 手动排序支持组内移动；目录分组支持跨组移动文件，文件类型分组会限制不成立的跨组移动。

### 工作集与会话恢复

- 新增工作区级“工作集”，可以保存：
    - 当前打开的标签。
    - 原生编辑器组及标签顺序。
    - 活动标签。
    - 手动分组及手动排序。
    - 标签和标签组固定状态。
    - 分组折叠状态。
    - 当前分组模式和排序模式。
- 可从命令面板或垂直标签栏创建、加载、覆盖、重命名和删除工作集。
- 加载前列出可能关闭的标签及未保存标签，默认保护未保存标签和固定标签。
- 原路径丢失时，仅在工作区存在唯一同名文件时自动关联，避免错误恢复。

### 标签状态展示
- 注：这部分功能未全部测试
- 新增资源只读状态，包括文件系统只读、权限只读以及 VS Code 只读规则。
- 新增资源不存在、无访问权限和文件系统不可用状态。
- 文件删除、恢复、权限或只读配置改变后会刷新状态。
- 标签右侧统一显示预览、固定、只读、未保存、资源异常及无法跳转状态。
- 未保存状态集中显示在关闭按钮附近。
- 标签文字区域获得更多可用宽度；关闭按钮仅在悬停或键盘焦点进入时显示。

### 布局、位置与启动入口

- 新增 `verticalTabs.position`，可把垂直标签栏放在编辑器区域左侧或右侧，并即时切换。
- 新增 `verticalTabs.toolbarPosition`，可将工具区固定在标签列表上方或下方。
- 状态栏右侧新增常驻显示/隐藏按钮；图标会根据标签栏位置和可见状态变化。
- 最终界面采用 VS Code 主题色和 Codicon 操作按钮。

### 键盘与无障碍操作
- 鼠标焦点在垂直标签页空白位置时，支持方向键、`Home`、`End` 和 `Enter` 浏览、激活标签。
	- 实用性不强，因为使用键盘按键移动、激活标签页后，鼠标的焦点就变为标签页内部，后续无法再在垂直标签页中移动
- 标签和分组菜单支持菜单键、`Shift+F10`、方向键、`Enter`、空格及 `Esc`。


## [0.2.1] - 2026-07-23

将新版README打包到版本中

## [0.2.0] - 2026-07-23

完成初版
