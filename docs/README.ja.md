
# エディタ領域の垂直タブ

```html
<p align="center">
  <a href="README.md">English</a> ·
  <a href="README.zh-CN.md">简体中文（规范源）</a> ·
  <a href="docs/README.zh-TW.md">繁體中文</a> ·
  <a href="docs/README.ja.md">日本語</a> ·
  <a href="docs/README.ko.md">한국어</a> ·
  <a href="docs/README.es.md">Español</a> ·
  <a href="docs/README.fr.md">Français</a> ·
  <a href="docs/README.de.md">Deutsch</a> ·
  <a href="docs/README.ru.md">Русский</a>
</p>
```

VS Code の <mark>エディタ領域の左側</mark>に、常に表示される<mark>垂直タブバー</mark>を表示します。メインサイドバーとセカンダリサイドバーを占有しません。

インターフェイスのレイアウトは以下の通りです：

```text
メインサイドバー | 垂直タブバー | エディタ領域 | セカンダリサイドバー
```

## デモ

![demo.gif](media/demo.gif)

## この拡張機能を開発した理由

VS Code はデフォルトで水平タブバーを使用します。多数のファイルを開くと、タブ名が切り詰められやすく、ファイルの検索や切り替えが直感的ではありません。

多くの垂直タブ拡張機能はタブリストをメインサイドバーに配置しますが、メインサイドバーはファイルエクスプローラー、検索、ソース管理、拡張機能なども表示する必要があります。

ユーザーがサイドバーの機能を切り替えると、垂直タブも一緒に非表示になります。

この拡張機能は垂直タブバーをエディタ領域の左側に配置するため、メインサイドバーで他の機能に切り替えても、垂直タブは表示されたままになります。

## 対象ユーザー

- 多数のファイルを同時に開くことが多い方
- 画面に十分な水平方向のスペースがある方
- 垂直タブがメインサイドバーを占有するのを避けたい方

## 機能

- **エディタ領域の左側に垂直タブを表示**
- 多言語対応 (i18n)
- タブグループ対応（自動グループ分けと手動グループ分け：タイプ別、親ディレクトリ別、VS Code の水平タブバーに追従）
- 手動ソート、名前順ソート、時間順ソート
- 垂直タブバーの表示/非表示
- 基本的なタブ操作：
	- ドラッグでグループ化
	- 一括クローズ
	- すべて展開
	- すべて折りたたむ
	- 右クリックでタブ・タブグループを固定
	- 一括移動（Shift キーで複数選択）
- グループタイプが親ディレクトリの場合、ファイルを他のグループにドラッグすると実際のファイルが移動します

## クイックスタート

- VS Code の拡張機能マーケットプレイスで "Vertical Tabs Inside Editor Area" を検索してインストールします。拡張機能の Identifier は `laikang287.vertical-tabs-inside-editor-area` です
- VS Code を再起動します
- VS Code のアクティビティバーにある `VERTICAL TABS` アイコンを見つけてクリックし、ビューを開きます。Show/Hide をクリックして垂直タブバーの表示/非表示を切り替えます
- 注 1：この `VERTICAL TABS` ビューはアクティビティバー内の他のよく使う場所に移動できます
	- 上記の GIF でデモをご覧ください
- 注 2：この拡張機能を使用する際は、VS Code のタブ折り返し機能をオフにすることをお勧めします：

```json
{
  "workbench.editor.wrapTabs": false
}
```

## インターフェイス言語の切り替え方法

設定項目 `verticalTabs.language` で拡張機能の言語を切り替えられます。デフォルト値は `auto` です。

## 仕組み

拡張機能の起動時に Webview が作成され、エディタ領域の一番左側の独立したエディタグループに配置されます。

この Webview は垂直タブの表示に使用されます。

その後、拡張機能は VS Code のエディタグループロック機能を使用してそのエディタグループをロックし、後続で開かれる新しいファイルが垂直タブバーのエディタグループに入らないようにします。

## 補足

1. 本プロジェクトの開発では、コードの記述、テスト、ドキュメント作成に AI プログラミングツールを活用しました
2. ドキュメントは README.zh-CN を基準とし、他の言語版は AI 翻訳によるものです
3. 簡体字中国語のドキュメントが本プロジェクトの主要メンテナンス版です

## ライセンス

MIT License - 詳細は [LICENSE](LICENSE) をご覧ください

## 手動インストール方法

- GitHub の [vscode-vertical-tabs-inside-editor-area](https://github.com/laikang287/vscode-vertical-tabs-inside-editor-area/tree/main/releases) リポジトリの releases ディレクトリから最新の `.vsix` ファイルをダウンロードします
- VS Code を開く → アクティビティバーの拡張機能ビューを開く → サイドバー右上の三点メニューをクリック → 「VSIX からインストール...」を選択
