# 编辑器区域垂直标签页

- 在vscode的<mark>编辑器区域的左侧</mark>显示垂直标签页
- 在vscode的界面显示为 ` 左侧边栏 | 垂直标签页 | 编辑器区域 | 右侧边栏`，普vscode的默认样式多出一栏`垂直标签栏`视图

## 当前功能

- VS Code 启动、安装后的窗口重载或下次启动时自动打开垂直标签栏
- 按原生编辑器组展示所有打开的标签，并实时同步活动、未保存、预览和固定状态
- 同名文件显示相对路径以消歧
- 文本、Diff、Notebook 和 Custom Editor 可点击切换；终端与其他扩展的 Webview 会显示但标记为不可跳转
- 支持关闭、关闭其他、关闭下侧、关闭已保存；批量关闭不会关闭原生固定标签
- 提供打开/聚焦、上一个标签、下一个标签命令；`Ctrl+Alt+V`（macOS 为 `Cmd+Alt+V`）聚焦垂直标签栏

P0 不支持自动收起、侧边栏位置切换、搜索和自定义分组。宽度由 VS Code 原生编辑器组分隔条调整。

## 解决的痛点
- 官方的标签栏是横向的，显示在顶部，标签一多就特别乱
- 其它插件的垂直标签栏都是在侧边栏显示，由于你会频繁在侧边栏切换功能（如切换到文件列表、搜索等），导致侧边栏的垂直标签栏无法固定显示

## 适合人群
- 用户会经常同时打开非常多个标签页，且屏幕宽度足够

## 功能说明

- 在编辑器区域左侧显示垂直标签页
- 可在编辑器区域和侧边栏之间切换显示位置
- 支持标签页分组，有包括自动分组、手动分组

## 原理

插件在 VS Code 启动后会创建一个 Webview，并在编辑器区域左侧打开它，
此时编辑器区域分为左右两个标签组，左标签组为垂直标签页，右标签组为其它，
随后将左标签页组（垂直标签页）锁定，锁定后，后续打开的新标签都会进入右标签组，
使用这种间接方式让 vscode多出一栏来显示`垂直标签页`

## 说明
本仓库完全右AI编写和整理的，不保证稳定性。


## ...... 分割线 .....
- 上方手工编写，普通用户查看
- 下方AI编写，或本人编写，提供给AI阅读


	
	
