# 编辑器区域垂直标签页

一个在 VS Code 编辑器区域打开垂直标签页 Webview 的扩展骨架。

当前版本实现启动时自动打开的单例占位视图和 `Vertical Tabs: Open` 命令。完整中文设计说明位于仓库根目录的 `README.zh-CN.md`。

## 开发

```bash
npm install
npm run verify
```

验证通过后，VSIX 位于 `dist/`。
