---
date created: 2026-07-23 03:12
author: null
tags: null
source: null
---

# 项目协作规范

## “计划模式”或“方案模式”字样的处理规则

- 如果用户提示词中带有“计划模式”或“方案模式”字样，但当前并未实际启用计划模式或方案模式，则按计划模式进行分析，但不得弹出选项要求用户选择。
- 必须在一次回复中列出所有需要用户确认的问题及其全部可选项，并明确标注每个问题的推荐选项。
- 问题按 `q1`、`q2`、`q3` 等编号，回复格式示例如下：

```text
- q1 你喜欢吃什么
1 苹果（推荐）
2 桃子
3 梨

- q2 你喜欢喝什么
1 水（推荐）
2 牛奶
3 椰汁
4 可乐
```

## 项目目标

这是一个 VS Code 扩展：在编辑器区域提供固定的垂直标签页体验。扩展在 VS Code 启动、安装后的窗口重载或下次启动时自动恢复垂直标签栏；VS Code 不支持新扩展于已运行窗口的安装瞬间自行打开视图。

## 功能规划与优先级

产品的核心差异是将垂直标签栏固定在编辑器区域左侧，而不是放入会被文件、搜索、源代码管理等视图频繁替换的侧边栏。所有设计应优先服务“打开大量文件时，快速、稳定、始终可见地导航编辑器标签”这一目标。

### P0：可用的垂直标签栏

- 实时同步打开、关闭、激活、编辑器分栏变化及未保存状态；状态变化必须立即反映到 Webview。
- 在编辑器区域左侧显示固定标签栏，使用独立且锁定的原生编辑器组；不得提供侧边栏位置切换。
- 标签栏首次使用默认宽度比例，之后恢复用户通过原生分隔条调节的上次宽度；无用户编辑器时先打开 VS Code 欢迎页，再在其左侧创建标签栏。
- 支持单击切换标签，以及关闭、关闭其他、关闭下侧、关闭已保存等基础操作；批量关闭不影响原生固定标签。
- 清晰区分 VS Code 的多个编辑器组；切换标签后聚焦其原始编辑器组。
- 对同名文件显示相对路径等消歧信息，不能只显示文件名。
- 展示所有编辑器标签；文本、Diff、Notebook、Custom Editor 以及 VS Code 可定位到的内置标签应尽量支持跳转。
- 提供命令面板与快捷键入口：打开/聚焦垂直栏、上一个/下一个标签；`Ctrl+Alt+V`（macOS 为 `Cmd+Alt+V`）聚焦垂直栏。

### P1：高效整理与批量操作

- 支持固定标签：默认不受“关闭其他”和批量关闭操作影响。
- 支持手动分组：创建、重命名、折叠、删除分组，以及将标签移动到分组。
- 支持拖拽移动至手动分组和移动至其他编辑器组；若 VS Code API 无法可靠支持拖拽，必须提供等价的菜单或命令操作。
- 活动栏只作为启动器容器；点击图标打开启动器，再由其中命令显示或关闭编辑器区域的垂直标签栏。

### P2：增强能力（不阻塞首版）

- 工作集/会话：保存一组标签及其分组，以便稍后恢复任务上下文。
- 智能分组建议：可基于目录、共同打开记录或 Git 改动给出建议，但不得在未确认时自动重排用户内容。
- 悬停预览：显示完整路径、Git 状态和最近访问时间；代码内容预览仅在性能可控时实现。

### 分组与状态规则

- 关闭标签不得删除其所属的空分组；空分组由用户显式删除。

- 固定和分组等 UI 偏好应持久化，并明确区分用户级设置与工作区级设置。

## 目录职责

- `src/`：扩展宿主代码和 Webview TypeScript 源码。
- `media/`：Webview 样式和静态资源。
- `test/unit/`：不启动 VS Code 的单元测试。
- `test/integration/`：通过 Extension Host 运行的集成测试。
- `scripts/`：构建、集成测试启动及 VSIX 内容校验脚本。
- `out/`：构建输出，不提交。
- `dist/`：可安装的 VSIX 输出，不提交。
- `todo_list.csv`：用户反馈问题、功能点和文档事项的登记表，按功能点分类并去重维护。

## 默认修改与交付边界

- 未经用户明确要求，不得修改 README、CHANGELOG、`docs/` 下的项目说明或发布文档，也不得为配合代码改动自动更新这些文档。
- 未经用户明确要求，不得修改 `package.json` 或 `package-lock.json`。如果任务必须修改受保护文件才能完整实现，应先说明原因并取得用户明确授权，不得绕过、部分实现或顺带修改。
- 用户明确要求修改的协作规则文件（例如 `AGENTS.md`）不受上述项目文档保护限制。
- `package.nls*.json` 属于产品本地化清单，不视为上述受保护的 package 清单或锁文件；功能明确涉及命令、配置或其它用户可见文本时，应按 i18n 规则同步修改。
- 默认只修改实现目标所必需的代码、测试和本地化资源，并执行与改动范围相称的类型检查或针对性测试。
- 默认不更新 `todo_list.csv`、不递增版本号、不执行完整 `npm run verify`、不构建或校验 VSIX，也不暂存文件或创建 Git 提交；这些动作仅在用户明确要求时执行。
- 任务完成前仍应执行只读的 `git status` 与 `git diff --check`，确认改动范围和补丁格式；不得借此修改或清理用户已有改动。

## Todo 登记规则

- 仅当用户明确要求登记、维护或更新 Todo 时，才允许新增、修改或更新根目录下的 `todo_list.csv`；不得根据分支、代码改动或功能完成情况自动更新。
- 用户明确要求新增 Todo 时，登记以对话窗口为单位；同一对话窗口最多新增一条。
- `todo_list.csv` 必须统一使用 UTF-8 编码（无 BOM）；后续发现该文件变为其它编码时，必须先转换回 UTF-8，转换时不得改变记录内容、列结构或原有行尾格式。
- 同一对话窗口后续无论用户追加、调整或要求实现其它代码改动，都不得再次新增记录；功能完成后不得自动更新首次登记记录的解决状态。
- 纯咨询、只读检查、审查、代码改动、协作规则或文档修改均不自动触发登记。
- `todo类型` 只能填写 `bug`、`功能点` 或 `文档`。
- 登记时按功能点分类；同一事项被多次反馈时只保留一条记录，不重复新增。
- 判断是否重复时优先比较 todo 类型、功能点、问题现象或事项内容以及触发条件；若只是描述措辞不同但根因、用户可见现象或待办目标一致，视为同一个事项。
- 新增记录时填写 todo 的基本信息：登记时间、todo类型、功能点、问题摘要、触发条件、影响范围、是否解决、解决时间、人工确认是否解决、AI确认是否解决和备注。
- `是否解决过` 默认填 `否`；修复完成后改为 `是` 并填写 `解决时间`。
- `人工确认是否解决` 只能在用户明确确认问题已解决后填写；不得由 AI 代填。
- `AI确认是否解决` 只能在用户要求 AI 检查或确认是否解决时填写；如果用户没有提出确认/检查要求，不主动确认，也不更新该列。

## 开发、测试与打包

- 安装依赖：`npm install`
- 编译与类型检查：`npm run compile`
- 单元测试：`npm run test:unit`
- Extension Host 集成测试：`npm run test:integration`
- 全部测试：`npm test`
- 递增版本号：每次构建 VSIX 前必须先更新 `package.json` 的 `version`，不得重复产出同一版本号的安装包。
- 构建 VSIX：`npm run package`
- 完整验收：`npm run verify`

调试时在 VS Code 中打开项目并按 F5；`.vscode/launch.json` 会以 Extension Development Host 启动扩展。

## 默认验证与按需交付流程

每项任务完成后，默认执行与改动范围相称的类型检查或针对性测试，并执行 `git status` 与 `git diff --check`。不得仅因功能完成而自动执行完整验收、打包、版本更新、暂存或提交。

用户明确要求构建 VSIX 或发布时，必须依序执行：

1. 递增 `package.json` 的 `version`；每次构建都必须使用新版本号，VSIX 文件名必须与该版本号一致。用户明确要求构建或发布即视为授权本次版本号更新。

2. 执行 `npm run verify`；任何失败都必须先修复，不能交付。

3. 确认 `dist/vertical-tabs-inside-editor-area-<version>.vsix` 已生成且 VSIX 内容校验通过。

4. 执行 `git status` 与 `git diff --check`，确认只包含任务相关改动；不得提交 `node_modules/`、`out/` 或 `dist/*.vsix`。

5. 在交付说明中写明执行过的测试和生成的 VSIX 绝对路径。

只有用户明确要求暂存或提交时，才执行相应 Git 写操作。提交时使用 Conventional Commit；初版工程固定使用 `chore: scaffold VS Code extension`，后续用户可见功能使用 `feat:`，修复使用 `fix:`，Git 提交说明使用中文。只暂存任务相关文件，不得包含用户已有的无关改动。

只要用户明确要求变更 `package.json` 版本号，就必须执行 `npm run verify` 构建新 VSIX，除非用户同时明确指明本次不需要构建。

版本号分 3 段：第 1 段由用户手工自增，第 2 段在新增功能时自增，第 3 段在修复 bug 时自增。

## VS Code 与 Webview 边界

- 扩展标识为 `local.vertical-tabs-inside-editor-area`，最低 VS Code 版本为 `^1.128.0`。
- 编辑器交互与业务状态留在扩展宿主；Webview 只负责渲染和发送已定义的消息。
- Webview 必须使用 nonce、严格 CSP、最小脚本权限，且仅加载由 `asWebviewUri` 生成的本地资源。
- 对所有来自 Webview 的消息先进行运行时校验；未知或无效消息不得改变状态。
- 禁止通过 Webview 读取、模拟或覆盖用户工作区文件。

## i18n 与文档翻译
- 插件内部所有用户可见功能都必须自动支持 i18n。新增或修改功能时，应在同一次改造中同步接入现有 i18n 机制并补齐仓库支持的全部界面语言，不得等待用户另行提出翻译要求。适用范围包括 Webview、扩展宿主提示、命令、菜单、配置项标题与说明、输入框、按钮、警告与错误提示、无障碍标签等；仅供开发者排查的内部诊断日志可单独评估。
- 插件内所有用户可见文本都必须通过统一的 i18n 机制提供，不得新增硬编码的用户可见文本。
- 新增或修改用户可见文本时，必须保留可靠的默认语言回退；相关测试应覆盖翻译键完整性和回退行为。
- 文档默认不进行 i18n。除非用户明确手工提出文档翻译要求，否则 AI 不得自动翻译、新增或同步更新任何译文。目前纳入文档翻译的类型包括 README 和 CHANGELOG，后续增加其它类型时也应在本节明确其规范源、目标语言和触发范围。
- `README.zh-CN.md` 文件只能由用户手工编写，AI 不可以修改此文档。
- `README.zh-CN.md` 是 README 的中文规范源。用户要求“翻译文档”时，将它翻译到仓库支持的所有其它 README 语言版本；用户只要求“翻译 README”时，仅翻译 README，不得同时翻译 CHANGELOG 或其它文档。
- `CHANGELOG.zh-CN.md` 是 CHANGELOG 的中文规范源，只能由用户手工编写和提交，AI 不得创建、修改或代替用户提交该文件。
- `CHANGELOG.md` 是由 `CHANGELOG.zh-CN.md` 翻译得到的英文版本；其它语言的 CHANGELOG 统一放在 `docs/` 目录中，文件名分别为 `CHANGELOG.zh-TW.md`、`CHANGELOG.ja.md`、`CHANGELOG.ko.md`、`CHANGELOG.es.md`、`CHANGELOG.fr.md`、`CHANGELOG.de.md` 和 `CHANGELOG.ru.md`。
- 仓库文档当前支持英语、简体中文、繁体中文、日语、韩语、西班牙语、法语、德语和俄语。后续增加或移除文档语言时，必须同步更新 README 与 CHANGELOG 的目标文件清单及语言导航。
- AI 不得因功能完成、版本更新、打包、发布或其它代码改动自动更新任何 CHANGELOG 译文；仅当用户明确要求“翻译文档”或“翻译 CHANGELOG”时才更新。用户只要求“翻译 CHANGELOG”或“翻译 CHANGELOG.md”时，将 `CHANGELOG.zh-CN.md` 翻译到上述全部其它 CHANGELOG 语言版本，不得同时翻译 README 或其它文档。
- 用户要求“翻译文档”时，执行当前纳入文档翻译范围的全部类型：将 `README.zh-CN.md` 翻译到仓库支持的所有其它 README 语言版本，并将 `CHANGELOG.zh-CN.md` 翻译到仓库支持的所有其它 CHANGELOG 语言版本。若任一中文规范源不存在，应报告缺失，不得自行补写规范源。
- README 顶部必须保留 GitHub 可用的语言导航链接，至少覆盖仓库当前支持的所有 README 语言版本。用户明确要求将 `README.zh-CN.md` 翻译为其它语言 README 时，其它语言文件中的以下导航块必须保持原样，不得翻译链接文字、路径、标点或标签结构：

```
[English](README.md) · [简体中文（规范源）](README.zh-CN.md) · [繁體中文](docs/README.zh-TW.md) · [日本語](docs/README.ja.md) · [한국어](docs/README.ko.md) · [Español](docs/README.es.md) · [Français](docs/README.fr.md) · [Deutsch](docs/README.de.md) · [Русский](docs/README.ru.md)
```

- CHANGELOG 顶部必须保留 GitHub 可用的语言导航链接，至少覆盖仓库当前支持的所有 CHANGELOG 语言版本。用户明确要求将 `CHANGELOG.zh-CN.md` 翻译为其它语言 CHANGELOG 时，所有语言文件中的以下导航块必须保持原样，不得翻译链接文字、路径、标点或标签结构：

```
[English](CHANGELOG.md) · [简体中文（规范源）](CHANGELOG.zh-CN.md) · [繁體中文](docs/CHANGELOG.zh-TW.md) · [日本語](docs/CHANGELOG.ja.md) · [한국어](docs/CHANGELOG.ko.md) · [Español](docs/CHANGELOG.es.md) · [Français](docs/CHANGELOG.fr.md) · [Deutsch](docs/CHANGELOG.de.md) · [Русский](docs/CHANGELOG.ru.md)
```


## 输出
- 只有本次实际构建了 VSIX 时，才在原本输出的最后用代码块给出本次在 `dist/` 目录中生成的 VSIX 文件绝对路径；未构建时不得输出历史 VSIX 路径或占位路径。
