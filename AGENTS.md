# 项目协作规范

## 项目目标

这是一个 VS Code 扩展：在编辑器区域提供固定的垂直标签页体验。当前版本是可运行骨架，只实现启动时打开的单例 Webview 与 `verticalTabs.open` 命令；真实标签同步、位置切换及分组属于后续功能。

## 功能规划与优先级

产品的核心差异是将垂直标签栏固定在编辑器区域左侧，而不是放入会被文件、搜索、源代码管理等视图频繁替换的侧边栏。所有设计应优先服务“打开大量文件时，快速、稳定、始终可见地导航编辑器标签”这一目标。

### P0：可用的垂直标签栏

- 实时同步打开、关闭、激活、编辑器分栏变化及未保存状态；状态变化必须立即反映到 Webview。
- 在编辑器区域左侧显示固定标签栏，并保留切换到侧边栏位置的兼容选项。
- 支持展开、收起、悬停临时展开和用户拖动调节宽度；收起时保留可辨识的窄栏或图标入口。
- 支持单击切换标签，以及关闭、关闭其他、关闭右侧、关闭已保存等基础操作。
- 清晰区分 VS Code 的多个编辑器组；切换标签后聚焦其原始编辑器组。
- 对同名文件显示相对路径等消歧信息，不能只显示文件名。
- 提供命令面板与快捷键入口：打开/聚焦垂直栏、上一个/下一个标签、切换收起状态、搜索标签。

### P1：高效整理与批量操作

- 支持模糊搜索打开的标签，以及按“已修改”“当前编辑器组”“固定标签”筛选。
- 支持固定标签：固定项置顶，且默认不受“关闭其他”和批量关闭操作影响。
- 支持手动分组：创建、重命名、折叠、删除分组，以及将标签移动到分组。
- 支持自动分组：按工作区根目录、父目录、文件类型和 Git 仓库分组。
- 支持按最近使用（默认）、文件名、路径或文件类型排序；固定项始终优先。
- 显示有价值的状态徽标：已修改、Git 状态、诊断错误/警告、只读及预览标签。
- 支持拖拽排序、移动至手动分组和移动至其他编辑器组；若 VS Code API 无法可靠支持拖拽，必须提供等价的菜单或命令操作。

### P2：增强能力（不阻塞首版）

- 工作集/会话：保存一组标签及其分组，以便稍后恢复任务上下文。
- 智能分组建议：可基于目录、共同打开记录或 Git 改动给出建议，但不得在未确认时自动重排用户内容。
- 悬停预览：显示完整路径、Git 状态和最近访问时间；代码内容预览仅在性能可控时实现。
- 标签统计与清理：显示打开数量、未保存数量，并提供“关闭未修改”“关闭非当前组”等操作。

### 分组与状态规则

- 手动分组优先于自动分组。
- 用户手动移动过的标签不得再被自动规则移动，除非用户显式恢复自动管理。
- 关闭标签不得删除其所属的空分组；空分组由用户显式删除。
- 固定、分组、排序和筛选等 UI 偏好应持久化，并明确区分用户级设置与工作区级设置。

### 首版范围控制

首个可用版本只交付以下能力：实时同步、编辑器区固定标签栏、收起/调宽、标签切换和关闭、多编辑器组识别、同名消歧、搜索及固定标签。不要在首版实现文件树、完整 Git 面板或复杂工作区管理，以免与 VS Code Explorer 重叠并削弱垂直标签导航的核心价值。

## 目录职责

- `src/`：扩展宿主代码和 Webview TypeScript 源码。
- `media/`：Webview 样式和静态资源。
- `test/unit/`：不启动 VS Code 的单元测试。
- `test/integration/`：通过 Extension Host 运行的集成测试。
- `scripts/`：构建、集成测试启动及 VSIX 内容校验脚本。
- `out/`：构建输出，不提交。
- `dist/`：可安装的 VSIX 输出，不提交。

## 开发、测试与打包

- 安装依赖：`npm install`
- 编译与类型检查：`npm run compile`
- 单元测试：`npm run test:unit`
- Extension Host 集成测试：`npm run test:integration`
- 全部测试：`npm test`
- 构建 VSIX：`npm run package`
- 完整验收：`npm run verify`

调试时在 VS Code 中打开项目并按 F5；`.vscode/launch.json` 会以 Extension Development Host 启动扩展。

## 完成功能的强制流程

每项独立功能完成后，必须依序执行：

1. `npm run verify`；任何失败都必须先修复，不能提交。
2. 确认 `dist/vertical-tabs-in-editor-area-<version>.vsix` 已生成且 VSIX 内容校验通过。
3. 执行 `git status` 与 `git diff --check`，只暂存该功能相关文件；不得提交 `node_modules/`、`out/` 或 `dist/*.vsix`。
4. 使用 Conventional Commit 自动创建 Git 提交。初版工程固定使用 `chore: scaffold VS Code extension`；后续用户可见功能使用 `feat:`，修复使用 `fix:`。
5. 在交付说明中写明执行过的测试和生成的 VSIX 绝对路径。

## VS Code 与 Webview 边界

- 扩展标识为 `local.vertical-tabs-in-editor-area`，最低 VS Code 版本为 `^1.128.0`。
- 编辑器交互与业务状态留在扩展宿主；Webview 只负责渲染和发送已定义的消息。
- Webview 必须使用 nonce、严格 CSP、最小脚本权限，且仅加载由 `asWebviewUri` 生成的本地资源。
- 对所有来自 Webview 的消息先进行运行时校验；未知或无效消息不得改变状态。
- 禁止通过 Webview 读取、模拟或覆盖用户工作区文件。
